# Test 2 — Postgres connection. Single source for BOTH docker-compose.yml
# (the container's credentials) and run.py (the client connection).
# Copy to .env before running:   cp .env.example .env
PGHOST=localhost
PGPORT=5433
PGUSER=criterion
PGPASSWORD=criterion
PGDATABASE=criterion
